# RENVIS Link Page

GitHub Pages用の1ページサイトです。

## 公開手順
1. GitHubで新しいリポジトリを作成
2. `index.html` と `renvis-logo.jpg` をアップロード
3. リポジトリの `Settings` → `Pages`
4. `Deploy from a branch` を選択
5. Branchを `main`、フォルダを `/ (root)` にして保存

数分後にGitHub PagesのURLが発行されます。

## 編集箇所
リンク、住所、電話番号などは `index.html` 内で変更できます。
